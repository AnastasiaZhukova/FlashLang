<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="colorPrimaryDark">@color/colorBeautifulPurple</item>
        <item name="android:windowBackground">@color/colorBeautifulPurple</item>
    </style>

    <style name="AppTheme.Day">
        <item name="colorPrimary">@color/colorPlatinum</item>
        <item name="colorPrimaryDark">@color/colorBeautifulPurple</item>
        <item name="colorAccent">@color/colorDarkTangerine</item>
    </style>

    <style name="AuthFields">
        <item name="android:layout_width">@dimen/auth_fields_width</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="AuthLowTextStyle">
        <item name="android:layout_gravity">center|start</item>
        <item name="android:layout_margin">16dp</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
